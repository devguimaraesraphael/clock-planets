#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")"

if [ ! -d node_modules ]; then
  echo "Instalando dependências..."
  npm install
fi

echo "Iniciando o Astrolábio das Horas..."
npm run dev
